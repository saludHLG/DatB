/* DatB admin geography location editors: provinces, municipalities and health centers. */
function openProvModal(id){const x=GEO.getProvs().find(p=>p.id===id);if(!x)return;$a('modal-provincia-title').innerHTML='<i class="bi bi-pencil"></i> Editar provincia';$a('prov-edit-id').value=x.id;$a('prov-nombre').value=x.nombre;$a('prov-codigo').value=x.codigo;$a('prov-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-provincia')).show();}
function openMunModal(id){const x=GEO.getMuns().find(m=>m.id===id);if(!x)return;$a('modal-municipio-title').innerHTML='<i class="bi bi-pencil"></i> Editar municipio';$a('mun-edit-id').value=x.id;$a('mun-nombre').value=x.nombre;fillProvSelect($a('mun-provincia'),x.provincia_id);$a('mun-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-municipio')).show();}
function openCentroModal(id){const x=GEO.getCentros().find(c=>c.id===id);if(!x)return;const m=GEO.getMuns().find(y=>y.id===x.municipio_id);$a('modal-centro-title').innerHTML='<i class="bi bi-pencil"></i> Editar centro de salud';$a('centro-edit-id').value=x.id;$a('centro-nombre').value=x.nombre;$a('centro-tipo').value=x.tipo||'';fillProvSelect($a('centro-provincia'),m?.provincia_id);fillMunSelect($a('centro-municipio'),m?.provincia_id,x.municipio_id);$a('centro-municipio').disabled=false;$a('centro-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-centro')).show();}

function saveProvincia(){const n=$a('prov-nombre').value.trim(),c=$a('prov-codigo').value.trim().toUpperCase();$a('prov-err').classList.add('d-none');if(!n||!c)return showLocErr('prov-err','Nombre y código son obligatorios.');if(!/^[A-Z]{2,3}$/.test(c))return showLocErr('prov-err','El código debe tener 2–3 letras mayúsculas.');const a=GEO.getProvs(),id=Number($a('prov-edit-id').value);if(a.some(x=>x.nombre.toLowerCase()===n.toLowerCase()&&x.id!==id))return showLocErr('prov-err','Ya existe una provincia con ese nombre.');if(a.some(x=>x.codigo===c&&x.id!==id))return showLocErr('prov-err','Ese código ya está en uso.');if(id){const i=a.findIndex(x=>x.id===id);a[i]={...a[i],nombre:n,codigo:c};}else a.push({id:nextGeoId(a),nombre:n,codigo:c});GEO.saveProvs(a);bootstrap.Modal.getInstance($a('modal-provincia'))?.hide();renderProvs();toast(id?'Provincia actualizada.':'Provincia creada.');}
function saveMunicipio(){const n=$a('mun-nombre').value.trim(),p=Number($a('mun-provincia').value);$a('mun-err').classList.add('d-none');if(!n)return showLocErr('mun-err','El nombre es obligatorio.');if(!p)return showLocErr('mun-err','Seleccione una provincia.');const a=GEO.getMuns(),id=Number($a('mun-edit-id').value);if(a.some(x=>x.nombre.toLowerCase()===n.toLowerCase()&&x.provincia_id===p&&x.id!==id))return showLocErr('mun-err','Ya existe ese municipio en esa provincia.');if(id){const i=a.findIndex(x=>x.id===id);a[i]={...a[i],nombre:n,provincia_id:p};}else a.push({id:nextMunId(a,p),nombre:n,provincia_id:p});GEO.saveMuns(a);bootstrap.Modal.getInstance($a('modal-municipio'))?.hide();renderMuns();toast(id?'Municipio actualizado.':'Municipio creado.');}
function saveCentro(){const n=$a('centro-nombre').value.trim(),t=$a('centro-tipo').value,m=Number($a('centro-municipio').value);$a('centro-err').classList.add('d-none');if(!n)return showLocErr('centro-err','El nombre es obligatorio.');if(!t)return showLocErr('centro-err','Seleccione el tipo de centro.');if(!m)return showLocErr('centro-err','Seleccione el municipio.');const a=GEO.getCentros(),id=Number($a('centro-edit-id').value);if(id){const i=a.findIndex(x=>x.id===id);a[i]={...a[i],nombre:n,tipo:t,municipio_id:m};}else a.push({id:nextGeoId(a),nombre:n,tipo:t,municipio_id:m});GEO.saveCentros(a);bootstrap.Modal.getInstance($a('modal-centro'))?.hide();renderCentros();toast(id?'Centro actualizado.':'Centro creado.');}

document.addEventListener('DOMContentLoaded',()=>{
    $a('btn-new-provincia')?.addEventListener('click',()=>{$a('modal-provincia-title').innerHTML='<i class="bi bi-map"></i> Nueva provincia';$a('prov-edit-id').value='';$a('prov-nombre').value='';$a('prov-codigo').value='';$a('prov-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-provincia')).show();});
    $a('btn-save-provincia')?.addEventListener('click',saveProvincia);
    $a('btn-new-municipio')?.addEventListener('click',()=>{$a('modal-municipio-title').innerHTML='<i class="bi bi-signpost-split"></i> Nuevo municipio';$a('mun-edit-id').value='';$a('mun-nombre').value='';fillProvSelect($a('mun-provincia'));$a('mun-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-municipio')).show();});
    $a('btn-save-municipio')?.addEventListener('click',saveMunicipio);
    $a('btn-new-centro')?.addEventListener('click',()=>{$a('modal-centro-title').innerHTML='<i class="bi bi-hospital"></i> Nuevo centro de salud';$a('centro-edit-id').value='';$a('centro-nombre').value='';$a('centro-tipo').value='';fillProvSelect($a('centro-provincia'));$a('centro-municipio').innerHTML='<option value="">— Seleccione provincia —</option>';$a('centro-municipio').disabled=true;$a('centro-err').classList.add('d-none');bootstrap.Modal.getOrCreateInstance($a('modal-centro')).show();});
    $a('centro-provincia')?.addEventListener('change',function(){fillMunSelect($a('centro-municipio'),this.value);$a('centro-municipio').disabled=!this.value;});
    $a('btn-save-centro')?.addEventListener('click',saveCentro);
});
